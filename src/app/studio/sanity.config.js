import { defineConfig } from 'sanity'

export default defineConfig({
    name: 'default',
    title: 'Portfolioblog',
    projectId: 'vkbsjv6h',
    dataset: 'production',
    plugins: [],
    schema: {
        types: [],
    },
})
